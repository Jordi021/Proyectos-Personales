<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.personalprojects"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31"
        android:theme="@style/Theme.PersonalProjectsJP">  <activity
        android:name=".MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistroActivity" />

        <activity
            android:name=".ProyectosActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".EditorProyectoActivity"
            android:label="@string/title_activity_add_project" />
        <activity
            android:name=".ActividadesActivity"
            android:label="@string/title_activity_project_detail" />
        <activity
            android:name=".EditorActividadActivity"
            android:label="@string/title_activity_add_task" />
    </application>

</manifest>